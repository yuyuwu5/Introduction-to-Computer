Class bomb{
	field int x,y;
	field int t;
	constructor putbomb(int Ax,int Ay){
		let x=Ax;
		let y=Ay;
		let t=0;
		do draw();
		return this;
	}
	method draw(){
		do Screen.setColor(true);
		do Screen.drawCircle(x+8,y+8,8);
	}
	method explode(){
	
	}
}